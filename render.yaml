services:
  - type: web
    name: your-react-flask-app
    env: python
    buildCommand: "pip install -r backend/requirements.txt && npm install --force && npm run build"
    startCommand: "python backend/app.py"
    plan: free